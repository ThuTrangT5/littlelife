{
  repository(owner: $owner, name:$respository) {
    issues(states: $status first: 100) {
      totalCount
      pageInfo {
        endCursor
      }
      nodes {
        number
        id
        title
        state
        createdAt
        comments {
          totalCount
        }
      }
    }
  }
}
