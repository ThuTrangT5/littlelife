{
  repository(owner: $owner, name:$respository) {
    issues(states: $status, last: 100) {
      totalCount
      pageInfo {
        endCursor
      }
      nodes {
        number
        id
        title
        state
        createdAt
        comments {
          totalCount
        }
      }
    }
  }
}
